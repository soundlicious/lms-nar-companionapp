ktor {
    deployment {
        port = 8080
        host = "0.0.0.0"

    }
    application {
        modules = [ lms.nar.companion.api.ApplicationKt.mainModule ]
    }
}

jdbc {
    username = "root"
    password = ${DB_PASSWORD}
    driverClassName = "com.mysql.cj.jdbc.Driver"
    url = "jdbc:mysql://localhost:3306/narcompanionapp"
}